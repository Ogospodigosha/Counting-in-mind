import React from 'react';
import {TaskObjType} from "../App";
type TestPropsType ={
    allTasks: TaskObjType[]
}
export  const Test = (props: TestPropsType) => {
    return (
        <div>

        </div>
    );
};

